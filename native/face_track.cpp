#include <iostream>

int main() {
    // OpenCV C++ is optional for this project. This placeholder keeps the native
    // interface stable when face tracking is not compiled with OpenCV support.
    std::cout << "[]\n";
    return 0;
}
